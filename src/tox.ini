[tox]
envlist = lint

[testenv:lint]
description = linter
skip_install = true
changedir = {toxinidir}
deps =
    black
    isort
commands =
    isort application/ --check-only
    black --check application/ -l 140
